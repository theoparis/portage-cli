//! Resolution-policy and plan layer for the
//! [`em`](https://github.com/lu-zero/portage-cli) Portage CLI — **placeholder,
//! not yet populated**.
//!
//! This crate name is reserved ahead of a planned migration: turning
//! repository facts + configuration into a solved, ordered merge plan via the
//! [`portage-atom-pubgrub`](https://crates.io/crates/portage-atom-pubgrub)
//! solver bridge — USE/keyword/mask policy, root-aware post-solve trimming,
//! and plan assembly — currently living in `portage-cli`'s
//! `query::depgraph` module. See that project's `todo/PENDING.md` for the
//! staged migration plan. Nothing here yet.
